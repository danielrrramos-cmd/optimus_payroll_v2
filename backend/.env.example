APP_ENV=prod
APP_SECRET=7b9ed92da26e87c564d821cf319a3415

# PostgreSQL — obtén estos datos de Supabase → Settings → Database → Connection Pooling (Transaction, puerto 6543)
DATABASE_URL="postgresql://postgres.XXXXXXXXXX:TU_PASSWORD@aws-0-eu-west-1.pooler.supabase.com:6543/postgres?sslmode=require&serverVersion=15"

# JWT — no cambies estas rutas, las claves ya están en el repositorio
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
JWT_PASSPHRASE=494363afa72028f7613fe92d758a10dadea9bde92117399aeaf63e45d2376f31

# CORS — en producción permite el dominio de Render (o .* para TFG)
CORS_ALLOW_ORIGIN=.*
